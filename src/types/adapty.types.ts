export interface PremiumState {
  isPremium: boolean;
  expiresAt: number | null;
  productId: string | null;
}
export interface PremiumState {
  isPremium: boolean;
  expiresAt: number | null;
  productId: string | null;
}

